# Optional application-specific CSP example.
# Do not include this blindly. Start in Report-Only mode, observe violations,
# then adapt allowed sources to the application before enforcing it.

add_header Content-Security-Policy-Report-Only "default-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'self'; form-action 'self';" always;
